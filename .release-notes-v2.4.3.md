# Meta_Kim v2.4.3

## Fixed

- Codex project skills now sync only to `.agents/skills/`, matching the current project-level Codex skill root.
- Update/sync removes the legacy Meta_Kim-managed `.codex/skills/meta-theory` mirror and deletes `.codex/skills` only when empty, so user-owned skills are preserved.
- Setup checks, validators, footprint reporting, command docs, and README guidance now agree on `.agents/skills/` as the single Codex project skill root.
- `@modelcontextprotocol/sdk` now requires `^1.29.0`, so fresh installs resolve patched transitive packages for the npm audit findings seen during release verification.

## Verification

- `npm run meta:sync`
- `npm run meta:check:runtimes`
- `npm run meta:validate`
- `npm run meta:setup:check`
- `npm run meta:check:sync-coverage`
- `node scripts/run-node-tests.mjs "tests/setup/sync-runtimes-manifest.test.mjs" "tests/setup/capability-index-inheritance-chain.test.mjs"`
- `npm run meta:graphify:rebuild`
- `npm run meta:graphify:check`
